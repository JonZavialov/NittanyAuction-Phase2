# NittanyAuction-Phase2

## README TODO:

- Brief explanation of how to get postgres db up and running
- Explain that the schema is automatically created on startup
- Copy .env.example into .env
- Explain that app.py just defines the API, needs to be run with `uvicorn app:app --reload` (This hot reloads the API)
